import Game from './lib/Game';

// const game = new Game(players);

// while (!game.over) {
//     displayTileOptions(game.turn.placementOptions);

//     let option = getUserInput(game.players[game.playerIndex]);

//     const placement = game.turn.placementOptions[option];

//     option = displayFollowerOptions(placement.getFollowerOptions());

//     placement.setFollower(placement.getFollowerOptions()[option])

//     game.turn.act(placement);
// }